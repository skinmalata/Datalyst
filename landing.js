(()=>{const page=document.querySelector('#landingPage'),open=()=>{page.classList.add('hidden');location.hash='workspace'};document.querySelectorAll('[data-open-workspace]').forEach(button=>button.onclick=open);document.querySelectorAll('[data-auth="login"]').forEach(button=>button.onclick=()=>window.datalystAuth?.login?.()||toast('Finish Auth0 setup in runtime-config.js to enable login.'));document.querySelectorAll('[data-auth="signup"]').forEach(button=>button.onclick=()=>window.datalystAuth?.signup?.()||toast('Finish Auth0 setup in runtime-config.js to enable sign-up.'));document.querySelectorAll('[data-contact]').forEach(button=>button.onclick=()=>toast('Contact sales at hello@datalyst.example'));if(location.hash==='#workspace')open()})();
