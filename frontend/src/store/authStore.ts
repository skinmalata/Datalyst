"use client";import {create} from "zustand";type Auth={ready:boolean;authenticated:boolean;token:string;organizationId:string;login?:()=>Promise<void>;signup?:()=>Promise<void>;logout?:()=>Promise<void>;setSession:(token:string,organizationId:string)=>void;setReady:(ready:boolean)=>void;setActions:(login:()=>Promise<void>,signup:()=>Promise<void>,logout:()=>Promise<void>)=>void;clear:()=>void};export const useAuthStore=create<Auth>(set=>({ready:false,authenticated:false,token:"",organizationId:"",setSession:(token,organizationId)=>set({token,organizationId,authenticated:true}),setReady:ready=>set({ready}),setActions:(login,signup,logout)=>set({login,signup,logout}),clear:()=>set({token:"",organizationId:"",authenticated:false})}));
