"use client";import {useAuthStore} from "@/store/authStore";export default function Login(){const {ready,login,signup}=useAuthStore();return <main className="grid min-h-screen place-items-center"><div className="text-center"><h1 className="text-3xl font-bold">Log in to Datalyst</h1><p className="my-4 text-text-secondary">Sign in to securely upload and analyse your data.</p><button disabled={!ready||!login} onClick={()=>login?.()} className="rounded bg-primary px-4 py-2 disabled:opacity-50">Log in</button><button disabled={!ready||!signup} onClick={()=>signup?.()} className="ml-3 rounded border border-border px-4 py-2 disabled:opacity-50">Create account</button></div></main>}
