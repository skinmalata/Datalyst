"use client";import Link from "next/link";import {AuthGuard} from "@/components/AuthGuard";import {DataUploader} from "@/components/DataUploader";import {ChatInterface} from "@/components/ChatInterface";import {useStore} from "@/store/useStore";export default function Dashboard(){const hasData=useStore(s=>!!s.datasetId);return <AuthGuard><main className="min-h-screen bg-background"><header className="flex items-center justify-between border-b border-border p-5"><Link href="/"><b>DATALYST</b></Link><button onClick={()=>useStore.getState().reset()} className="rounded border border-border px-3 py-2">Reset workspace</button></header><section className="p-6">{hasData?<ChatInterface/>:<DataUploader/>}</section></main></AuthGuard>}
