import { generateSuggestedQueries } from "@/lib/query-suggestions";
test("generates ten executive-ready source-aware questions", () => { const suggestions = generateSuggestedQueries([{ Date: "2026-01-01", Region: "West", Sales: "120" }]); const text = suggestions.map((item) => item.question).join(" "); expect(suggestions).toHaveLength(10); expect(text).toContain("Sales"); expect(text).toContain("Region"); expect(text).toContain("over time"); expect(text).toContain("expect"); });
