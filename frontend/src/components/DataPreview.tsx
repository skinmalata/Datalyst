export function DataPreview({rows}:{rows:Record<string,unknown>[]}){const columns=Object.keys(rows[0]||{});return <div className="overflow-auto rounded-xl border border-border"><table className="w-full text-left text-sm"><thead><tr>{columns.map(column=><th className="p-3" key={column}>{column}</th>)}</tr></thead><tbody>{rows.slice(0,20).map((row,index)=><tr className="border-t border-border" key={index}>{columns.map(column=><td className="p-3" key={column}>{String(row[column]??"")}</td>)}</tr>)}</tbody></table></div>}
