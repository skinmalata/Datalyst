"use client";import {useRef} from "react";import {Upload} from "lucide-react";import {api} from "@/lib/api-client";import {parseDataFile} from "@/lib/file-parser";import {useStore} from "@/store/useStore";
export function DataUploader(){const input=useRef<HTMLInputElement>(null),setDataset=useStore(s=>s.setDataset);const upload=async(file:File)=>{const rows=parseDataFile(await file.text(),file.name);const saved=await api<{id:string}>("/api/datasets",{method:"POST",body:JSON.stringify({name:file.name,rows,classification:"internal"})});const profile=await api<{records:number;columns:number;completeness:number;duplicateRows:number;warnings:string[]}>("/api/datasets/"+saved.id+"/profile");setDataset(rows,saved.id,profile)};return <section className="mx-auto max-w-2xl rounded-2xl border border-dashed border-primary bg-surface p-12 text-center"><Upload className="mx-auto mb-4 text-primary"/><h2 className="text-2xl font-bold">Add your data</h2><p className="mt-2 text-text-secondary">Choose a CSV or JSON file. Datalyst checks the quality of your data before you ask questions.</p><input ref={input} className="hidden" type="file" accept=".csv,.json" onChange={e=>e.target.files?.[0]&&upload(e.target.files[0]).catch(error=>alert(error.message))}/><button className="mt-6 rounded-lg bg-primary px-5 py-3 font-semibold" onClick={()=>input.current?.click()}>Choose file</button></section>}
