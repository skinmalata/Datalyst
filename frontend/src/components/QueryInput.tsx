"use client";import {useState} from "react";export function QueryInput({onSubmit,disabled}:{onSubmit:(value:string)=>void;disabled?:boolean}){const [value,setValue]=useState("");return <form className="flex gap-2" onSubmit={e=>{e.preventDefault();if(value.trim()){onSubmit(value);setValue("")}}}><textarea className="min-h-12 flex-1 rounded-lg border border-border bg-background p-3 text-white" value={value} disabled={disabled} placeholder="Ask a question about your data…" onChange={e=>setValue(e.target.value)}/><button disabled={disabled} className="rounded-lg bg-primary px-5 disabled:opacity-50">Send</button></form>}
