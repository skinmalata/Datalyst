"use client";import {api} from "@/lib/api-client";import {useStore} from "@/store/useStore";import {QueryInput} from "./QueryInput";import {ChartViewer} from "./ChartViewer";
const questions=[["Which pages get the most traffic?","Find the pages people open most often."],["Which pages have the highest bounce rate?","Find pages where visitors leave quickly."],["Show me outliers or anomalies","Find unusual numbers worth checking."],["Show me trends over time","See whether your numbers rise or fall."]];
export function ChatInterface(){const {datasetId,messages,add,setLoading,loading}=useStore();const ask=async(question:string)=>{add({id:crypto.randomUUID(),role:"user",content:question});setLoading(true);try{const plan=await api<any>("/api/analyses/plan",{method:"POST",body:JSON.stringify({datasetId,question})});const result=await api<any>("/api/analyses",{method:"POST",body:JSON.stringify({datasetId,question,plan})});const output=result.result;const values=output.values||[];add({id:crypto.randomUUID(),role:"assistant",content:values.length?"I found "+values.length+" results. The first is "+values[0].label+" with "+values[0].value+". "+output.method+".":"The answer is "+output.value+". "+output.method+".",values})}catch(error){add({id:crypto.randomUUID(),role:"assistant",content:error instanceof Error?error.message:"Analysis could not be completed."})}finally{setLoading(false)}};return <div className="mx-auto flex max-w-4xl flex-col gap-5"><div className="grid gap-2 sm:grid-cols-2">{questions.map(([question,description])=><button key={question} onClick={()=>ask(question)} className="rounded-lg border border-border bg-surface p-4 text-left"><b>{question}</b><span className="mt-1 block text-sm text-text-muted">{description}</span></button>)}</div>{messages.map(message=><article key={message.id} className={message.role==="user"?"ml-12 rounded-xl bg-primary p-4":"mr-12 rounded-xl bg-surface p-4"}><p>{message.content}</p>{message.values?.length?<ChartViewer data={message.values}/>:null}</article>)}{loading?<p className="text-text-muted">Checking your data…</p>:null}<QueryInput disabled={!datasetId||loading} onSubmit={ask}/></div>}
